
function generateur(p,q){
    let  n, z, d = 0, e, i;

    n = p * q;
    z = (p - 1) * (q - 1);

    e=5;
   d=euclideEtendu(e,z).r

    let t =[e,d]
    return t
}

export default generateur;